name: Generate README

on:
  push:
    branches-ignore:
      - 'robot-docs'
  workflow_dispatch:

jobs:
  call-update-readme:
    uses: PepperDash/PepperDash/workflow-templates/.github/workflows/update-readme.yml@add-feedboacks
    with:
      target-branch: ${{ github.ref_name }}
